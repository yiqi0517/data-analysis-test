# 9.16 课后作业第 1、2 题

## 1. 代数证明题

$$\mathrm{RSS}(\beta_0, \beta_1) = \sum_{i=1}^{n} e_i^2 = \sum_{i=1}^{n} (y_i - \beta_0 - \beta_1 x_i)^2$$

$$\frac{\partial \mathrm{RSS}}{\partial \beta_0} = -2\sum_{i=1}^{n}(y_i - \beta_0 - \beta_1 x_i) = 0$$

$$\therefore \sum_{i=1}^{n}(y_i - \beta_0 - \beta_1 x_i) = 0$$

即

$$\sum_{i=1}^{n} e_i = 0$$

$$\frac{\partial \mathrm{RSS}}{\partial \beta_1} = -2\sum x_i(y_i - \beta_0 - \beta_1 x_i) = 0$$

$$\therefore \sum x_i(y_i - \beta_0 - \beta_1 x_i) = 0$$

即

$$\sum_{i=1}^{n} x_i e_i = 0$$

## 2. 理解思考题

$$R^2 = 1 - \frac{\mathrm{RSS}}{\mathrm{TSS}}$$

$$\mathrm{RSS} = \sum (y_i - \hat{y}_i)^2$$

当自变量数量增加时，RSS 不变或减小，

而 $\mathrm{TSS} = \sum (y_i - \bar{y})^2$ 保持不变。

$$\therefore \frac{\mathrm{RSS}}{\mathrm{TSS}} \text{ 不变或减小}$$

$$\therefore R^2 \text{ 不变或上升}$$

$$R^2_{adj} = 1 - \frac{\mathrm{RSS}/(n-k-1)}{\mathrm{TSS}/(n-1)}$$

当自变量数量 $k$ 增加时，RSS 基本不变，残差自由度 $(n-k-1)$ 变小。

$$\therefore \frac{\mathrm{RSS}}{n-k-1} \uparrow \qquad \therefore \frac{\mathrm{RSS}/(n-k-1)}{\mathrm{TSS}/(n-1)} \uparrow \qquad \therefore R^2_{adj} \text{ 下降}$$
